local Library = {}

function Library:CreateWindow(hubName)
    if not game:IsLoaded() then game.Loaded:Wait() end

    local CoreGui = game:GetService("CoreGui")
    local UserInputService = game:GetService("UserInputService")
    local TweenService = game:GetService("TweenService")
    local RunService = game:GetService("RunService")
    local Players = game:GetService("Players")
    local localPlayer = Players.LocalPlayer or Players:GetPropertyChangedSignal("LocalPlayer"):Wait()

    local TargetParent = CoreGui
    local status, err = pcall(function() Instance.new("Folder", CoreGui):Destroy() end)
    if not status then TargetParent = localPlayer:WaitForChild("PlayerGui") end

    local GuiName = "BacBeoHub_Official_UI"
    for _, child in pairs(CoreGui:GetChildren()) do if child.Name:find("BacBeoHub") then child:Destroy() end end

    local ScreenGui = Instance.new("ScreenGui", TargetParent)
    ScreenGui.Name = GuiName
    ScreenGui.ResetOnSpawn = false
    ScreenGui.DisplayOrder = 999999

    local MainFrame = Instance.new("Frame", ScreenGui)
    MainFrame.Name = "Main"
    MainFrame.Size = UDim2.new(0, 550, 0, 380)
    MainFrame.Position = UDim2.new(0.5, -275, 0.5, -190)
    MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    MainFrame.BorderSizePixel = 0
    MainFrame.ClipsDescendants = true
    MainFrame.Active = true

    Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)
    local UIStroke = Instance.new("UIStroke", MainFrame)
    UIStroke.Color = Color3.fromRGB(0, 255, 100)
    UIStroke.Thickness = 1.5

    local TopBar = Instance.new("Frame", MainFrame)
    TopBar.Size = UDim2.new(1, 0, 0, 50)
    TopBar.BackgroundTransparency = 1

    local title = Instance.new("TextLabel", TopBar)
    title.Size = UDim2.new(0, 250, 1, 0)
    title.Position = UDim2.new(0, 20, 0, 0)
    title.BackgroundTransparency = 1
    title.Text = hubName
    title.TextColor3 = Color3.fromRGB(255, 255, 255)
    title.TextSize = 22
    title.Font = Enum.Font.GothamBold
    title.TextXAlignment = Enum.TextXAlignment.Left

    local leftContainer = Instance.new("Frame", MainFrame)
    leftContainer.Size = UDim2.new(0, 150, 1, -65)
    leftContainer.Position = UDim2.new(0, 10, 0, 55)
    leftContainer.BackgroundTransparency = 1
    Instance.new("UIListLayout", leftContainer).Padding = UDim.new(0, 8)

    local rightContainer = Instance.new("ScrollingFrame", MainFrame)
    rightContainer.Size = UDim2.new(1, -180, 1, -65)
    rightContainer.Position = UDim2.new(0, 170, 0, 55)
    rightContainer.BackgroundTransparency = 1
    rightContainer.CanvasSize = UDim2.new(0, 0, 0, 0)
    rightContainer.AutomaticCanvasSize = Enum.AutomaticSize.Y
    rightContainer.ScrollBarThickness = 3
    Instance.new("UIListLayout", rightContainer).Padding = UDim.new(0, 8)

    local isFirstFolder = true
    local windowMethods = {}

    function windowMethods:CreateFolder(folderName)
        local folderBtn = Instance.new("TextButton", leftContainer)
        folderBtn.Size = UDim2.new(1, 0, 0, 36)
        folderBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        folderBtn.Text = "  " .. folderName
        folderBtn.TextColor3 = isFirstFolder and Color3.fromRGB(0, 255, 100) or Color3.fromRGB(160, 160, 160)
        folderBtn.Font = Enum.Font.SourceSansBold
        Instance.new("UICorner", folderBtn).CornerRadius = UDim.new(0, 6)

        local featureFrame = Instance.new("Frame", rightContainer)
        featureFrame.Size = UDim2.new(1, 0, 0, 0)
        featureFrame.AutomaticSize = Enum.AutomaticSize.Y
        featureFrame.BackgroundTransparency = 1
        featureFrame.Visible = isFirstFolder
        Instance.new("UIListLayout", featureFrame).Padding = UDim.new(0, 6)

        folderBtn.MouseButton1Click:Connect(function()
            for _, f in pairs(rightContainer:GetChildren()) do if f:IsA("Frame") then f.Visible = false end end
            for _, b in pairs(leftContainer:GetChildren()) do if b:IsA("TextButton") then b.TextColor3 = Color3.fromRGB(160, 160, 160) end end
            featureFrame.Visible = true
            folderBtn.TextColor3 = Color3.fromRGB(0, 255, 100)
        end)
        isFirstFolder = false

        local itemMethods = {}

        function itemMethods:Button(text, callback)
            local btn = Instance.new("TextButton", featureFrame)
            btn.Size = UDim2.new(1, -5, 0, 34)
            btn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
            btn.Text = text
            btn.TextColor3 = Color3.new(1,1,1)
            Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 5)
            btn.MouseButton1Click:Connect(callback)
        end

        function itemMethods:Toggle(text, callback)
            local state = false
            local tFrame = Instance.new("Frame", featureFrame)
            tFrame.Size = UDim2.new(1, -5, 0, 36)
            tFrame.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
            Instance.new("UICorner", tFrame).CornerRadius = UDim.new(0, 5)
            
            local btn = Instance.new("TextButton", tFrame)
            btn.Size = UDim2.new(0, 40, 0, 22)
            btn.Position = UDim2.new(1, -50, 0.5, -11)
            btn.BackgroundColor3 = Color3.fromRGB(55, 55, 55)
            btn.Text = ""
            Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 11)
            
            local circle = Instance.new("Frame", btn)
            circle.Size = UDim2.new(0, 16, 0, 16)
            circle.Position = UDim2.new(0, 3, 0.5, -8)
            Instance.new("UICorner", circle).CornerRadius = UDim.new(0, 8)

            btn.MouseButton1Click:Connect(function()
                state = not state
                btn.BackgroundColor3 = state and Color3.fromRGB(0, 200, 80) or Color3.fromRGB(55, 55, 55)
                circle.Position = state and UDim2.new(1, -19, 0.5, -8) or UDim2.new(0, 3, 0.5, -8)
                callback(state)
            end)
        end

        function itemMethods:Label(text)
            local lbl = Instance.new("TextLabel", featureFrame)
            lbl.Size = UDim2.new(1, -5, 0, 30)
            lbl.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
            lbl.Text = text
            lbl.TextColor3 = Color3.fromRGB(200, 200, 200)
            lbl.Font = Enum.Font.SourceSansItalic
            Instance.new("UICorner", lbl).CornerRadius = UDim.new(0, 5)
            return lbl
        end

        return itemMethods
    end
    return windowMethods
end

return Library
